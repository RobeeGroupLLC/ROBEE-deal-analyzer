{
  "name": "robee-group-deal-analyzer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check app.js && node --check calculations.js && node --check server.js"
  }
}
